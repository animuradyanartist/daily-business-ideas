node_modules/
.wrangler/
*.log

# Secrets — never commit real values. Copy .env.example to .env and fill it in.
.env
.env.*
!.env.example
.dev.vars

# Scout spend-ledger lock (held for milliseconds during a reservation)
evidence/budget/.lock
