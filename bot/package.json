{
  "name": "daily-business-ideas-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/parse.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
